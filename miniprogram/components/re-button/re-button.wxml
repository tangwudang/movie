<!--components/re-button/re-button.wxml-->

  <view class="button" id="{{target}}" bindtap="navToTarget">
    <image class="button-image" src="{{image == '' ? '' : image}}"> </image>
    <view class="button-title">{{title}}</view>
 </view>
